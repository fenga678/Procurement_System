<Window x:Class="CanteenProcurement.Wpf.Dialogs.TaskDetailsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="任务明细"
        Width="920"
        Height="620"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource AppWindowStyle}">
    <Border Style="{StaticResource CardStyle}" Margin="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="TitleText" Style="{StaticResource DialogTitleStyle}" />
            <TextBlock x:Name="SummaryText" Grid.Row="1" Style="{StaticResource DialogSubtitleStyle}" Margin="0,10,0,0" />
            <DataGrid x:Name="DetailsGrid" Grid.Row="2" Margin="0,18,0,0" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="序号" Binding="{Binding Seq}" Width="60" />
                    <DataGridTextColumn Header="分类" Binding="{Binding CategoryName}" Width="120" />
                    <DataGridTextColumn Header="商品名称" Binding="{Binding ProductName}" Width="180" />
                    <DataGridTextColumn Header="单位" Binding="{Binding Unit}" Width="80" />
                    <DataGridTextColumn Header="日期" Binding="{Binding PurchaseDate, StringFormat='yyyy-MM-dd'}" Width="120" />
                    <DataGridTextColumn Header="单价" Binding="{Binding Price, StringFormat='￥#,##0.00'}" Width="100" />
                    <DataGridTextColumn Header="数量" Binding="{Binding Quantity, StringFormat='N0'}" Width="90" />
                    <DataGridTextColumn Header="金额" Binding="{Binding Amount, StringFormat='N1'}" Width="100" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</Window>
